// This file will contain functions related to the raytracing itself


// Raytrace(camera, scene, width, height)
	// for each pixel, traces a ray and determines the object that intersects
	// finds the color using the intersection.

// Intersect(ray, scene)
 // finds the object in the scene that is closest to the camera and intersects the ray


// Intersect(ray, object)
	// determines the intersection point of the given object with the ray, if any